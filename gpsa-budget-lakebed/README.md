# GPSA Budget Lakebed

A Lakebed capsule version of the GPSA budget-flow visualization from `../edgar/gpsa_budget_flow.html`.

Run locally:

```sh
npx lakebed dev . --port 3017
```

Build an anonymous Lakebed artifact:

```sh
npx lakebed build . --target anonymous --json
```
